# Resume_Tracker
The AI Resume Analyzer is a fullstack web app where users upload their resume (PDF) and paste a job description. The app sends both to an AI (like Claude), which compares them and returns a match score (0–100%), missing keywords, and specific improvement tips. Results are saved per user so they can track progress over time.
